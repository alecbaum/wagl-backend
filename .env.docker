# Docker Development Environment Configuration
ASPNETCORE_ENVIRONMENT=Development
ASPNETCORE_URLS=http://+:8080

# Database Configuration
POSTGRES_CONNECTION=Host=postgres;Port=5432;Database=wagldb;Username=waglmin;Password=localdev123;SSL Mode=Prefer;

# Redis Configuration
REDIS_CONNECTION=redis:6379

# JWT Configuration
JWT_SECRET_KEY=your-super-secure-secret-key-for-jwt-tokens-minimum-256-bits-long-for-hs256
JWT_ISSUER=WaglBackendAPI
JWT_AUDIENCE=WaglBackendClient
JWT_EXPIRATION_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRATION_DAYS=7

# Redis Cache Configuration
REDIS_KEY_PREFIX=wagl:dev:
REDIS_DEFAULT_EXPIRATION_MINUTES=30

# PostgreSQL Container Configuration
POSTGRES_DB=wagldb
POSTGRES_USER=waglmin
POSTGRES_PASSWORD=localdev123

# Optional Admin Tools
PGADMIN_DEFAULT_EMAIL=admin@wagl.com
PGADMIN_DEFAULT_PASSWORD=admin123