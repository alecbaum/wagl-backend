# Docker Production Environment Configuration
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:8080

# Database Configuration (Digital Ocean)
POSTGRES_CONNECTION=Host=your-production-db-host;Port=5432;Database=wagldb;Username=waglmin;Password=your-production-db-password;SSL Mode=Require;

# Redis Configuration (Use external Redis service in production)
REDIS_CONNECTION_STRING=your-production-redis-connection-string

# JWT Configuration (MUST be changed in production)
JWT_SECRET_KEY=change-this-to-a-secure-512-bit-key-in-production-environment
JWT_ISSUER=WaglBackendAPI
JWT_AUDIENCE=WaglBackendClient
JWT_EXPIRATION_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRATION_DAYS=7

# Redis Cache Configuration
REDIS_KEY_PREFIX=wagl:prod:
REDIS_DEFAULT_EXPIRATION_MINUTES=30

# Performance and Security Settings
ASPNETCORE_FORWARDEDHEADERS_ENABLED=true